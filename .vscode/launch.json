{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run any script",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/run.py",
            "args": ["${relativeFile}"],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
